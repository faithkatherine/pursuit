import { Layout } from "components/Layout";
import { View, Text } from "react-native";

export const Buckets = () => {
  return (
    <Layout>
      <View>
        <Text>Buckets</Text>
      </View>
    </Layout>
  );
};
