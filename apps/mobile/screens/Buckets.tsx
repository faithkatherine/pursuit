import { Layout } from "components/Layout";
import { View, Text } from "react-native";

const Buckets = () => {
  return (
    <Layout>
      <View>
        <Text>Buckets</Text>
      </View>
    </Layout>
  );
};
export default Buckets;
